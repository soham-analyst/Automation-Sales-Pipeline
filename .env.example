# Copy this file to ".env" and fill in your real values.
# ".env" itself is git-ignored (see .gitignore, Part 24) - never commit real credentials.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=sales_pipeline
DB_USER=sales_app_user
DB_PASSWORD=change_me
ENVIRONMENT=development